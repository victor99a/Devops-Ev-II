apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: greeting-app
  labels:
    app: backend
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "demo"
  SERVER_PORT: "8080"
  JAVA_TOOL_OPTIONS: "-XX:MaxRAMPercentage=75.0"
